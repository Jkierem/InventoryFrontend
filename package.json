{
  "name": "inventory-front",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "juanform": "latest",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router": "^5.0.1",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1",
    "styled-components": "^4.3.1",
    "serve": "^11.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:prod": "pm2 start ./server.js --name front",
    "stop": "pm2 stop front",
    "clean": "pm2 delete front",
    "restart": "pm2 restart front",
    "production": "yarn build && yarn start:prod &"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
